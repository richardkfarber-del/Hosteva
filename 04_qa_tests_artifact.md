<thinking>
I need to search for template literals in the `dashboard.html` file to verify if raw JavaScript is being rendered as plain text.
</thinking>

```json
{"name": "content_search", "arguments": {"pattern": r"template\s*literal", "path": "/app/templates/dashboard.html"}}
```