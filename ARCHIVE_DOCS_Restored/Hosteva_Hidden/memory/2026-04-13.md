
## 2026-04-13 13:49 EDT - Session Flush
- **Architecture Alignment**: The "Double Brain" (duplicate agent directories) issue is resolved by strictly passing absolute paths via the `cwd` parameter in `sessions_spawn` (e.g., `/home/rdogen/OpenClaw_Factory/projects/Hosteva/agents/hawkeye/`).
- **Sprint 4 Status**: Focusing on Airbnb/VRBO live sync (Spike 301), Gemini RAG, and Stripe Paywall. Spike 301 API contract established using `fastapi.BackgroundTasks` for ingestion and Vibranium Encryption for OAuth tokens.
- **Protocol Enforcement (The Fury Boundary & Lobster Protocol)**: The Orchestrator MUST NOT manually inject code or modify application files using write tools. Code must be output by executing agents as raw markdown with a `# FILE: path` tag, saved to a temporary workspace file, and processed via `scripts/lobster_interceptor.py`.
- **Bug/Error Handling Rule**: ANY build failure, QA block, or dependency error strictly requires a formal Bug Ticket (Hawkeye -> Coulson's Gate -> ARB Swarm Review -> Execution). Zero "quick fixes" or shadow patching allowed.
- **Celery Deprecation**: `celery` dependency officially removed from `app/routers/listings.py` in favor of native `fastapi.BackgroundTasks`.
