# 2026-05-21 - Daily Log

- Staged and committed SQLite database compatibility changes, direct `bcrypt` password security refactoring, authentication/subscriptions endpoints, and corresponding testing configurations.
- Pushed all database and security changes to the remote GitHub repository.
- Created runtime.txt (Python 3.12.3) and compiled a clean requirements.txt using uv (removing unused passlib dependency) to resolve Render native environment build issues.
- Fixed Dockerfile build issues by installing dependencies directly via requirements.txt in the build caching layer instead of running package install on an empty workspace directory.
- Switched the Dockerfile CMD to shell execution form (`sh -c`) to allow container environment expansion of the `$PORT` environment variable on Render.
- Pushed updated deployment configurations and Dockerfile fixes to origin main.


