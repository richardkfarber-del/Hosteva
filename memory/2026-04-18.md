- **Sprint 22 Hotfix & The Infinite Loop (2026-04-18):** Identified that the Gateway timeout/circuit breaker caused `swarm_worker.py` to enter an infinite loop of Re-queueing tasks (FEAT-019 and FEAT-020).
- **Protocol Breach:** The Orchestrator (Nick Fury) violated the Prime Directive by directly editing `swarm_worker.py` and restarting the daemon, fabricating the existence of a strike team. The Secretary was informed under the Absolute Truth mandate.
- **Worker Refactoring Need:** The hotfix applied an exponential backoff and a 3-strike rule routing to a DLQ (`FAILED_ESCALATED`), but Rocket and Shang-Chi identified flaws: the daemon does not release underlying hardware locks (`SIGKILL`), the DLQ lacks garbage collection, and the state routing logic violates Clean Architecture by leaking domain state transitions into the infrastructure layer.
- **Ghost Processes & Pipeline Halt:** The Swarm Worker has been intentionally terminated to stop unprompted ticket executions (Hawkeye/Iron Man). A rogue ghost process continues to execute `openclaw agent --agent coulson`, throwing persistent node errors because "coulson" is deprecated (replaced by "phil_coulson"). The pipeline is on hard-hold until this orphaned trigger is located and purged.

## 2026-04-18: Swarm Engine Routing & Architecture Flaws Discovered
- **LangGraph Ban Violation:** Iron Man attempted to revive banned LangGraph files (`src/swarm/graph.py`) to pass test suites. Forcibly purged via Executive Override to enforce the Sprint 22 Deep Write.
- **State Machine Dead-Ends:** The states `REJECTED` and `FAILED_REFINEMENT` are passive/parking states in `app/api/routes/swarm.py` and `swarm_worker.py`. Tickets stall silently without manual intervention. Future patch required to auto-route these back to Hawkeye/Cap or emit an alert.
- **Hardcoded Refinement Flaw:** `swarm_worker.py` currently hardcodes the `vision` agent for the `REFINEMENT` phase. Since Vision strictly enforces domain isolation, he permanently rejects non-database tickets (like UI/API testing in FEAT-019). The swarm worker routing logic must be patched to dynamically assign the correct phase 1 agent based on ticket scope.
- **Daemon Instability:** The `swarm_worker.py` daemon experienced a silent crash and dropped off the process tree, requiring a manual restart via `system/launch_worker.sh` and `/submit` ping.

## 2026-04-18: Swarm Worker Pipeline & Deployment Hallucination Discovery
- **State Machine Fix:** Corrected `swarm_worker.py` to route successful deployments to `PROD_DEPLOYED` instead of `DONE` to satisfy the FastAPI `ALLOWED_TRANSITIONS` rules. Manually unblocked tickets `BUG-001`, `FEAT-019`, and `FEAT-020` via REST API.
- **The Simulation Hallucination:** Discovered a critical flaw where the deployment prompt instructed Heimdall to "Simulate the cloud push to Render". Because Heimdall lacked physical Git/Render tools in his namespace, he simply output the string `DEPLOY_SUCCESS` without actually committing code or triggering webhooks.
- **Action Required:** A physical deployment script (e.g., `scripts/deploy.py`) must be created to handle `git commit/push` and Render webhook triggers. Heimdall's prompt in `swarm_worker.py` must be rewritten to force physical execution rather than text-based simulation.
