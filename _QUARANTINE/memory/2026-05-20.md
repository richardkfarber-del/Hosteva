# 2026-05-20 - Daily Log

- Resolved SQLite database compatibility issues in testing environment by dynamically ignoring PostgreSQL-specific features (Regex CheckConstraints, GIST Indexes, and range/JSONB types) when running SQLite.
- Fixed a passlib-bcrypt compatibility bug on Python 3.12 by refactoring password hashing and verification to use the `bcrypt` library directly.
- Standardized password hashing across the user and host routers.
- Verified that all 11 unit and integration tests inside the `tests/` directory pass successfully.
